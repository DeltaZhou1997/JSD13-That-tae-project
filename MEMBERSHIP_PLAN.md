# แผนสมาชิก "เบี้ย" ธาตุแท้ (That-Tae Membership)

> ค่าคงที่ทั้งหมดอยู่ที่ `server/src/utils/membership.js` และ `client/src/constants/membership.js` (สองไฟล์นี้ต้องตรงกันเสมอ)
> Server เป็นผู้คำนวณจริงทุกครั้ง ไม่เชื่อจำนวนเบี้ย/ส่วนลดที่ Frontend ส่งมา

## 1. กลุ่มลูกค้าเป้าหมาย

ลูกค้ารายได้ปานกลางถึงสูง ใส่ใจสุขภาพ ซื้อซ้ำเป็นประจำ (รายสัปดาห์) และให้ความสำคัญกับความคุ้มค่ามากกว่าราคาถูกที่สุด
แผนจึงออกแบบให้

- **ได้เบี้ยทุกบาท** ตั้งแต่ออเดอร์แรก (ไม่มียอดขั้นต่ำ) ให้เห็นผลเร็ว
- **กล่องใหญ่คุ้มกว่า** แบบเห็นได้ชัด เพื่อดันยอดต่อออเดอร์ (AOV) และการสมัครแพ็กเกจรายสัปดาห์
- **ระดับสมาชิกมีคุณค่า** ต้องซื้อต่อเนื่องจึงขึ้นระดับสูง แต่ขึ้นแล้วไม่ตกเมื่อใช้เบี้ย

## 2. การได้เบี้ย (หลังชำระเงินสำเร็จเท่านั้น)

### A La Carte
ทุก **฿10 = 1 เบี้ย** (ปัดเศษลง) คิดจากค่าสินค้า ไม่รวมค่าจัดส่ง

### กล่อง Cooking Kit รายสัปดาห์ (ไม่เป็นเชิงเส้น)

| กล่อง | ราคา | เบี้ยที่ได้ | ถ้าซื้อ A La Carte ยอดเท่ากัน | ความคุ้ม |
|---|---:|---:|---:|---:|
| SIZE S (4 ชุด) | ฿599 | **60** | 59 | x1.0 |
| SIZE M (6 ชุด) | ฿899 | **110** | 89 | x1.2 |
| SIZE L (8 ชุด) | ฿1,169 | **170** | 116 | x1.5 |
| SIZE XL (12 ชุด) | ฿1,599 | **280** | 159 | x1.75 |

- เมนูเสริมที่เกินโควตากล่อง คิดแบบ A La Carte (฿10 = 1 เบี้ย) บวกเพิ่ม
- จาก M ไป XL จ่ายเพิ่ม ฿700 แต่ได้เบี้ยเพิ่ม 170 (≈ ฿4.1 ต่อเบี้ย) เป็นแรงจูงใจให้เลือกกล่องใหญ่

### ตัวคูณตามระดับ และการใช้เบี้ยในออเดอร์เดียวกัน
- เบี้ยที่ได้ = เบี้ยพื้นฐาน × ตัวคูณของระดับ (ตารางข้อ 3)
- ถ้าใช้เบี้ยลดราคา เบี้ยที่ได้ลดลงตามสัดส่วนส่วนที่จ่ายด้วยเบี้ย (กันการใช้เบี้ยปั่นเบี้ย)
  - เช่น ค่าสินค้า ฿500 ใช้เบี้ยลด ฿50 → ได้เบี้ย 50 × (450/500) = 45

## 3. ระดับสมาชิก (ขึ้นอัตโนมัติ)

ขึ้นระดับจาก **เบี้ยสะสมตลอดชีพ (`lifetimePoints`)** คือเบี้ยทั้งหมดที่เคยได้ ไม่ลดลงเมื่อใช้เบี้ยแลกส่วนลด

| ระดับ | เบี้ยสะสมตลอดชีพ | ตัวคูณเบี้ย | ยอดใช้จ่ายโดยประมาณ* |
|---|---:|---:|---|
| **Bronze** | 0 | x1.0 | สมัครสมาชิก |
| **Silver** | 1,500 | x1.1 | ≈ ฿12,000 (กล่อง M ทุกสัปดาห์ ~3 เดือน) |
| **Gold** | 5,000 | x1.25 | ≈ ฿38,000 (กล่อง M/L ต่อเนื่อง ~8–9 เดือน) |
| **Platinum** | 12,000 | x1.5 | ≈ ฿80,000 (กล่อง L/XL ต่อเนื่อง ~1 ปี) |

\* ประมาณจากการซื้อกล่องแพ็กเกจ ถ้าซื้อ A La Carte อย่างเดียวต้องใช้จ่ายมากกว่านี้ (฿10 = 1 เบี้ย)

- ระบบอัปเกรดทันทีหลังได้เบี้ยจากออเดอร์ที่ชำระแล้ว
- **ไม่ลดระดับอัตโนมัติ** และถ้าแอดมินตั้งระดับให้สูงกว่าเกณฑ์ ระบบจะคงระดับนั้นไว้
- ผู้ใช้เก่าที่ยังไม่มี `lifetimePoints` ระบบใช้ยอดเบี้ยคงเหลือเดิมเป็นจุดเริ่มต้น

## 4. การใช้เบี้ยเป็นส่วนลดตอน Checkout

| กติกา | ค่า |
|---|---|
| อัตราแลก | **10 เบี้ย = ฿1** |
| ขั้นต่ำ | 10 เบี้ย |
| เพิ่ม/ลด | ทีละ 10 เบี้ย (ไม่มีเศษ) |
| ใช้ทั้งหมด | กดปุ่ม "ใช้ทั้งหมด" = เบี้ยสูงสุดที่ใช้ได้ ปัดลงเป็นหลักสิบ |
| เพดาน | ไม่เกินค่าสินค้า (**ค่าจัดส่งต้องจ่ายเสมอ**) |

ขั้นตอนฝั่ง Server
1. `priceOrder()` ตรวจจำนวนเบี้ยกับยอดคงเหลือจริงใน DB แล้วคำนวณส่วนลด/ยอดสุทธิ/เบี้ยที่จะได้
2. หักเบี้ยแบบ atomic (`points >= จำนวนที่ใช้`) ตอนสร้างคำสั่งซื้อ กันใช้ซ้ำจากหลายแท็บ
3. บันทึกไม่สำเร็จ → คืนเบี้ยทันที
4. แอดมินยกเลิกคำสั่งซื้อ → คืนเบี้ยที่ใช้ให้อัตโนมัติ (ครั้งเดียว, `pointsRefunded`)
5. ออเดอร์ Stripe ที่ยังไม่จ่าย เบี้ยจะถูกกันไว้จนกว่าจ่ายสำเร็จหรือแอดมินยกเลิก

ฟิลด์ใหม่ใน `Order`: `pointsRedeemed`, `pointsDiscount`, `pointsRefunded`
ฟิลด์ใหม่ใน `User`: `lifetimePoints`

## 5. ตัวอย่าง

| สถานการณ์ | ยอด | เบี้ยที่ได้ |
|---|---|---:|
| Bronze ซื้อ A La Carte ฿355 | ฿355 + ส่ง ฿120 | 35 |
| Bronze กล่อง M | ฿899 + ส่ง | 110 |
| Gold กล่อง XL | ฿1,599 + ส่ง | 350 (280 × 1.25) |
| Bronze กล่อง M + เมนูเสริม ฿250 | ฿1,149 + ส่ง | 135 |
| Silver ใช้ 500 เบี้ย กับ A La Carte ฿600 | ฿600 − ฿50 + ส่ง = ฿670 | 60 (60 × 1.1 × 550/600 ปัดลง) |

## 6. ไฟล์ที่เกี่ยวข้อง

| ส่วน | ไฟล์ |
|---|---|
| กติกา (Server) | `server/src/utils/membership.js` |
| คิดราคา/เบี้ย | `server/src/utils/orderPricing.js` |
| หัก/คืน/ให้เบี้ย + อัปเกรดระดับ | `server/src/routes/v2/checkout.routes.js` |
| Model | `server/src/models/Order.model.js`, `server/src/models/User.model.js` |
| ข้อมูลให้ AI Advisor | `server/src/services/advisor/knowledgeBase.js` |
| กติกา (Client) | `client/src/constants/membership.js` |
| UI ใช้เบี้ย | `client/src/components/checkout/CheckoutSummary.jsx` |
| สถานะสมาชิก | `client/src/components/checkout/CheckoutUserStatus.jsx`, `client/src/pages/ProfilePage.jsx` |
| เบี้ยต่อกล่อง | `client/src/components/checkout/PlanSelector.jsx` |

## 7. ต่อยอดในอนาคต (ยังไม่ได้ทำ)
- สิทธิ์ตามระดับเพิ่มเติม เช่น ส่งฟรีสำหรับ Platinum, เมนูพิเศษก่อนใคร
- เบี้ยหมดอายุ (เช่น 12 เดือน) เพื่อกระตุ้นการใช้
- โบนัสเบี้ยวันเกิด / กินครบตามธาตุเจ้าเรือน
- คืนเบี้ยที่ได้รับเมื่อมีการคืนเงิน (Refund)
